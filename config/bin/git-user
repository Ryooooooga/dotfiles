#!/usr/bin/env bash
if [ "$#" -eq 0 ]; then
    git config user.name
    git config user.email
    exit
fi

user="$(git config users."$1")" || (echo "No such user $1"; exit 1)
IFS=":" read -r name email <<< "$user"

git config user.name "$name"
git config user.email "$email"
