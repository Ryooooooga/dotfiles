#!/usr/bin/env -S zx --quiet
const message = typeof argv.m === "string" ? argv.m : "initial commit";

await $`git rev-parse`;

if ((await $`git rev-parse HEAD`.exitCode) === 0) {
    throw new Error("this is not an empty repository");
}

await $`git commit -m ${message} --allow-empty`;
